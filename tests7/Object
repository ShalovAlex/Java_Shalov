Структура Object

Метод - переопределение
String toString() - Обязательно
boolean equals(Object obj) - Обязательно
int hashCode() - Обязательно

void clone() - Можно, но не нужно. Лучше сделать копирующий
               конструктор.

Class getClass() - Метод финальный и нативный.

void wait() - Многопоточные методы. Финальные. Выходят за пределы курса.
void notify() - Многопоточные методы. Финальные. Выходят за пределы курса.
void notifyAll() - Многопоточные методы. Финальные. Выходят за пределы курса.

void finalize) - Даже не думай об этом.

EQUALS(O)
Метод сравнивает содержимое объектов и выводит значение типа boolean.
Оператор == сравнивает ссылки на объекты, а не сами объекты.
Должен переопределяться вместе с методом hashCode().
Есть ряд обязательных проверок, которые должны быть реализованы при переопределении метода.

equals и hashCode и toString - всегда переопределяем
Реализация equals должна учитывать три свойства: рефлексивность, транзитивность, симметричность.

String toString() — возвращает строку, описывающий объект
boolean equals(Object obj) — определяет, равен ли один объект другому с точки
зрения состояния / содержимого, поэтому его нужно переопределять
void finalize() — вызывается перед удалением неиспользуемого объекта, сейчас
не рекомендуется к использованию
Class<?> getClass() — получает класс объекта во время выполнения
int hashCode() — возвращает хеш-код, связанный с вызывающим объектом,
вычисляется на основе состояния / содержимого, его тоже нужно
переопределять
void notify() — возобновляет выполнение потока, который ожидает
вызывающего объекта
void notifyAll() — возобновляет выполнение всех потоков, которые ожидают
вызывающего объекта
void wait() — ожидает другого потока выполнения
void wait(long millis) — ожидает другого потока выполнения
void wait(long millis, int nanos) — ожидает другого потока выполнения
Object clone() — создаёт новый объект, не отличающийся от клонируемого

Есть обязательное правило — если переопределяется метод equals, то должен
переопределяться и метод hashCode

Это проверки на совпадение ссылок, null, соответствия типов
И уже далее ….
Приведение к типу класса, т.к. переопределяемый equals имеет параметр типа
Object